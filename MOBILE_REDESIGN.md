# WANDO Mobile 10/10 — спецификация (авторизовано: строить автономно, по завершении пуш)

Цель: Apple-уровень мобильного UX (≤680px и TMA). Десктоп НЕ трогаем.
Метод: волна → puppeteer-скрины 390×844 (dark/light) → фикс → следующая. В конце smoke2/3/4 + пуш.

## Волна 1 — Таб-бар 4+➕ и полноэкранный захват
- ≤680: скрыть текущий 8-табный бар; новый #mnav (fixed bottom, glass):
  [⚡ Сегодня=view now] [📁 Проекты=view mprojects] [➕ FAB-центр, приподнят, градиент лого]
  [📥 Приёмка=intake, только isTeam; гостю — 💡 Бэклог] [🤖 Вандо=vandoToggle()]
- ➕ → полноэкранный лист захвата #capSheet: крупный textarea (автофокус), 🎙 (существующий SR),
  живые чипы parseQuick, кнопки «⚡ Создать» (runCmd) и «📥 В Приёмку» (если cloudOn: insert intake
  через RPC cockpit_intake нельзя без ключа — использовать локальный путь: newTask). MVP: «Создать».
- Statusbar (легенда) на мобиле скрыть — шум.

## Волна 2 — Экран «Сегодня» (мобильный renderNow)
- В renderNow(): if window.innerWidth<=680 → упрощённый шаблон:
  герой-задача на 60vh: label, крупный title (34px), 2 кнопки (Фокус/Готово), чипы minimum;
  ниже «Ещё сегодня · N» — до 3 строк (tap=открыть);
  секция <details> «Подробности дня» = pulse-row + healcard + insights + momentum (свернуто).
  «Было без тебя» оставить сверху (1-2 строки).
- Убрать AI-План/Claude кнопки с мобильного «Сегодня» (есть Вандо-таб).

## Волна 3 — Свайп-жесты
- touch на .nh-up, «ещё сегодня» строках и .card (доска):
  вправо >80px → done (awardForDone+celebrateAt+save+render);
  влево >80px → end=завтра (start=min) toast «🌙 На завтра».
- Визуал: translateX + фон-подложка ✓/🌙 при свайпе (CSS ::before на контейнере свайпа).

## Волна 4 — «Проекты» и вкладки внутри проекта
- Новый view mprojects (только мобайл): дерево компаний (как portfolio prow, крупнее),
  tap → switchProject + setView('board')… НО прежде: внутри проекта сверху сегмент-контрол
  [Доска|Гант|Неделя|Бэклог|Команда] (горизонтальный скролл чипов) — заменяет скрытые табы.
  Реализация: #mseg fixed под шапкой на мобиле при view in (board,gantt,week,backlog,team).
- «Всё»-обзор с мобильного корня убрать (доступен из Проекты-экрана кнопкой «Пульс компании»).

## Волна 5 — Полировка + TMA-нативность
- Типографика: h1 32-34px, отступы ×1.5, один акцент на экран.
- Пружинные переходы экранов (view fade/slide 160ms var(--ease)).
- TMA: если tmaInitData(): подключить https://telegram.org/js/telegram-web-app.js,
  Telegram.WebApp.ready()+expand(); MainButton «＋ Задача»→capSheet; BackButton→закрыть sheet/карточку;
  themeParams НЕ переопределяют бренд (только bg headerColor).
- Финал: smoke2/3/4 зелёные, скрины всех волн, git push (авторизован), TELEGRAM_BOT.md строку про TMA.

## Правила
- Каждая волна = отдельный коммит. Десктоп-регрессию проверять шотом 1280px «Сейчас»+Доска.
- Грабли: flex-shrink детей; z-слои (mnav z=36, capSheet z=70, drawer 51, vando panel 66);
  кириллица без \b; backdrop-filter не на предков fixed.
